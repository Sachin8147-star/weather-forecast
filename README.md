# Weather Forecast (Python)

A Python program that fetches weather data
using OpenWeather API and visualizes it
using Matplotlib and Seaborn.

## Run
```bash
pip install requests matplotlib seaborn
python weather.py